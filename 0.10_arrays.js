var num = [8,6,7,5,3,0,9];
var sum = 0;
function sumer (x) {
	for(var i = 0; i < x.length; i ++){
		sum = sum + x[i];

	}
	return sum
}

// .push() function sends to end of array
// .pop() removes last item of array
// .slice() removes outside
// .splice() removes item from array
// .indexOf() shows index # in array

console.log(sumer(num));

var 
for (var i = 0; i < 9; i++){

}